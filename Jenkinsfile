#!groovy

node {
    
    stage("Start") {
        sh "echo I can has sucjbjbnububyhbcess?"
        def testImg = docker.build()
    }
    
    stage("End") {
        sh "echo Nothing to see here"   
    }
}
